<h1 class="text-center">New Booking <small><%= "#{@experience.title} (#{@experience.time.strftime("%I:%M %p")}~)" %></small></h1>

<div class = "container">
  <div class = "col-md-offset-4 col-md-4"  id="booking_form">
    <%= render 'form' %>
  </div>
</div>
<!-- <%= link_to 'Back', bookings_path %> -->
