<div class="container">

<h1>New Booking <small><%= "#{@experience.title} (#{@experience.time.strftime("%I:%M %p")}~)" %></small></h1>
<%= render 'form' %>

<%= link_to 'Back', bookings_path %>

</div>
