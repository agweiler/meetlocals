<div class="container" style="text-align:center">
  <% if host_signed_in? %>
    <h1>Listing bookings to your experiences</h1>
  <% elsif guest_signed_in? %>
    <h1>Listing your bookings</h1>
  <% elsif admin_signed_in? %>
    <h1>Listing all bookings</h1>
  <% else %>
    <h1>Log in to display your bookings</h1>
  <% end %>
  <% if @bookings.empty? %>
    <h2>Nothing was found D:</h2>
  <% else %>
  </br>
  <div class="col-sm-11" style="border: 2px solid; border-radius: 10px; border-color: #000000">
    <table class="table">
      <thead>
        <tr>
          <th>Date</th>
          <th>Time</th>
          <!-- <th>End time</th> -->
          <% if guest_signed_in? %>
            <th>Host</th>
          <% elsif host_signed_in? %>
            <th>Guest</th>
          <% else %>
            <th>Host</th>
            <th>Guest</th>
          <% end %>
          <th colspan="3">Experience</th>
          <th>Status</th>
          <th>Size</th>
          <th colspan="2"></th>
        </tr>
      </thead>
      <tbody>
        <% @bookings.each do |booking| %>
          <% experience = Experience.find(booking.experience_id)%>
          <tr>

            <td><%= booking.date.strftime('%b %d, %Y') %></td>
            <td><%= experience.time.strftime('%I:%M %p') %></td>
            <!-- <td><%= booking.end_time.strftime('%r') %></td> -->

            <% if guest_signed_in? %>
              <td><%= link_to "#{experience.host.username}", experience.host %></td>
            <% elsif host_signed_in? %>
              <td><%= link_to "#{booking.guest.username}", booking.guest %></td>
            <% else %>
              <td><%= link_to "#{experience.host.username}", experience.host %></td>
              <td><%= link_to "#{booking.guest.username}", booking.guest %></td>
            <% end %>
            <td colspan="3"><%= booking.experience.title %></td>
            <td><%= booking.status.capitalize %></td>
            <td><%= booking.group_size %></td>
            <td colspan="2"><%= link_to 'Show', booking, class:"btn btn-xs btn-primary", style:"color:#fff" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <% end %>
    <br>
  </div>
</div>
