<div class="row">
  <div id="pre_foot">
      <h1 class="text-center">FIND YOUR PERFECT HOST</h1>
  </div>
</div>

<div class="row" id="search_box">
  <div class="col-xs-10 col-xs-offset-1 text-center" >

  <%= form_tag hosts_search_path, class:"form-inline"  do %>
  <div class="input-group col-sm-2 col-xs-12 selectParent">
    <%= text_field :search, :date,
    {class:"form-control datepick fa fa-arrow-left", placeholder:"Date", style:"font-family: inherit; font-size: 14px; padding-bottom: 4px; color: #000 !important"} %>
  </div>

  <div class="input-group col-sm-2 col-xs-12 selectParent">
    <%= select :search, :age_range, Host.age_ranges,
      {selected: @selected_age}, {class:"form-control square_box"} %>
  </div>

  <div class="input-group col-sm-2 col-xs-12 selectParent">
    <%= select :search, :max_group, (["Guests"] + (1..10).to_a),
      {selected: @selected_group}, {class:"form-control square_box"} %>
  </div>

  <div class="input-group col-sm-2 col-xs-12 selectParent">
    <%= select :search, :location, Host.get_location,
      {selected: @selected_location}, {class:"form-control square_box", id:"location_box"} %>
  </div>

  <div class="input-group">
    <%= submit_tag :FILTER,
     {class:"btn btn-success btn-xl", style:"height:35px;"} %>
  </div>
  <% end %>

  </div>
</div>

<div class="container" style="margin-top:40px">

  <%= content_tag :div, render(@hosts), class:'row', id:'view' %>

  <!-- <% @hosts.in_groups_of(3, false).each do |group| %>
  <div class="row" id="hosts_row-<%=params[:page]%>">
    <% group.each do |host| %>
    <div class="col-md-4 col-sm-6">

      <%= render host %>

    </div>
    <% end %>
  </div>
  <% end %> -->

  <div class="text-center" id="infinite-scrolling" hidden>
    <%= will_paginate %>
  </div>

  <!-- <div class="text-center" id="btn_load">
    <button type="button" name="button" class="btn btn-default btn-lg learnmore" style="background-color:#fff; color:darkGreen;border:1;">LOAD</button>
  </div> -->
</div>

<script>
$(function(){
  $('.datepick').datepicker({
      clearBtn: false,
      format: "yyyy-mm-dd",
      keyboardNavigation: false
  });

  $('.datepick').datepicker('setDate', '<%= raw(@selected_date) %>')
});
</script>
