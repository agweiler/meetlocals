$('#view').append('<%= j render @hosts %>');
<% if @hosts.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @hosts %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
  $('#btn_load').remove();
<% end %>
