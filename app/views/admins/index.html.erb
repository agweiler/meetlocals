
</br>
</br>

<div class="container-fluid" style="text-align:center; background-color: #ffffff
; border: 2px solid; border-radius: 25px; border-color: #000000">
    <h1>Listing Admins</h1>
    <table class="table-striped">
      <thead>
        <tr>
          <th>Email</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @admins.each do |admin| %>
          <tr>
            <td><%= admin.email %></td>
            <td><%= link_to 'Show', admin %></td>
            <td><%= link_to 'Destroy', admin, method: :delete, data: { confirm: 'u r not ab00se rite?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

</br>
</div>

<div class="container-fluid" style="text-align:center; background-color: #ffffff
; border: 2px solid; border-radius: 25px; border-color: #000000">
    <h1>Listing Guests</h1>
    <table class="table-striped">
      <thead>
        <tr>
          <th>Username</th>
          <th>Email</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @guests.each do |guest| %>
          <tr>
            <td><%= guest.username %></td>
            <td><%= guest.email %></td>
            <td><%= link_to 'Show', guest %></td>
            <td><%= link_to 'Destroy', guest, method: :delete, data: { confirm: 'u r not ab00se rite?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

</br>
</div>
<br>


<div class="container-fluid" style="text-align:center; background-color: #ffffff
; border: 2px solid; border-radius: 25px; border-color: #000000">
    <h1>Listing Hosts</h1>

    <table class="table-striped">
      <thead>
        <tr>
          <th>Username</th>
          <th>Email</th>
          <th>Country</th>
          <th>State</th>
          <th>Suburb</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @hosts.each do |host| %>
          <tr>
            <td><%= host.username %></td>
            <td><%= host.email %></td>
            <td><%= host.country %></td>
            <td><%= host.state %></td>
            <td><%= host.suburb %></td>
            <td><%= link_to 'Show', host %></td>
            <td><%= link_to 'Destroy', host, method: :delete, data: { confirm: 'u r not ab00se rite?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </br>
</div>

<br>

