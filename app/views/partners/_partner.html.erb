
<div class="col-md-4 col-sm-6">  
  <div class="thumbnail" style = "margin-bottom:15px">
    <div class="caption" style="background-color: #f3f3f3; height: 100%">
      <div id="avataro">
        <%= link_to image_tag(partner.images.first.image_file.url(:thumb), class: "img-circle"), partner if partner.images.present? %>
      </div>
      <h3 class="host_name"><%= link_to partner.name,  partner_path(partner) %></h3> 
      <label> Address </label>
      <p><%= partner.address %> </p>
        <label> Contact Info </label>
      <p> <%= partner.contact_info %> </p>  
    </div>
  </div>
</div>