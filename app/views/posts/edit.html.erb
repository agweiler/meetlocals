<div class ="container">
	<div id="page_wrapper">
		<h1>Edit Post</h1>

		<%= render 'form' %>
	  <%=  link_to 'Back to blog', blog_path if @post.post_type == "blog" %> 
	  <%=  link_to 'Back to Press', press_path if @post.post_type == "press" %> 
	</div>
</div>